#include<opencv2/opencv.hpp>
#include<array>
#include<vector>
#include<iostream>
#include "EdgeTest.h"

using namespace cv;

int main()
{
  std::vector<int> numbers;
  numbers.push_back(1);
  std::cout << len(numbers);
}
